name: Test Makefile Build System

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

env:
  APP_NAME: simdokpol
  GO_VERSION: '1.21'

jobs:
  # --- TEST MAKEFILE SYNTAX & STRUCTURE ---
  test-makefile-syntax:
    name: Test Makefile Syntax & Structure
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Test Makefile syntax
        run: |
          echo "🔍 Testing Makefile syntax..."
          make -n --dry-run all > /dev/null 2>&1 || { echo "❌ Makefile syntax error in 'all' target"; exit 1; }
          make -n --dry-run clean > /dev/null 2>&1 || { echo "❌ Makefile syntax error in 'clean' target"; exit 1; }
          make -n --dry-run deps > /dev/null 2>&1 || { echo "❌ Makefile syntax error in 'deps' target"; exit 1; }
          echo "✅ Makefile syntax is valid"

      - name: Test Makefile targets
        run: |
          echo "📋 Available Makefile targets:"
          make list-targets || { echo "❌ list-targets failed"; exit 1; }
          
          echo "🔍 Testing critical targets..."
          make -n --dry-run windows 2>&1 || { echo "❌ windows target has issues"; exit 1; }
          make -n --dry-run linux 2>&1 || { echo "❌ linux target has issues"; exit 1; }
          make -n --dry-run clean 2>&1 || { echo "❌ clean target has issues"; exit 1; }
          echo "✅ Critical targets are valid"

      - name: Validate Makefile variables
        run: |
          echo "🔍 Validating Makefile variables..."
          if ! grep -q "APP_NAME :=" Makefile; then
            echo "❌ APP_NAME variable not found"
            exit 1
          fi
          if ! grep -q "BUILD_DIR :=" Makefile; then
            echo "❌ BUILD_DIR variable not found"
            exit 1
          fi
          if ! grep -q "VERSION_FULL :=" Makefile; then
            echo "❌ VERSION_FULL variable not found"
            exit 1
          fi
          echo "✅ All required variables are defined"

  # --- TEST DEPENDENCY INSTALLATION ---
  test-dependencies-linux:
    name: Test Linux Dependencies
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v4
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Test Linux dependency commands
        run: |
          echo "🔍 Testing dependency installation on Linux..."
          make -n --dry-run install-deps 2>&1 || { echo "❌ install-deps has syntax errors"; exit 1; }
          echo "✅ Linux dependency installation test passed"

      - name: Test Go dependencies
        run: |
          echo "🔍 Testing Go module dependencies..."
          make -n --dry-run deps 2>&1 || { echo "❌ deps target has issues"; exit 1; }
          echo "✅ Go dependencies test passed"

  test-dependencies-macos:
    name: Test macOS Dependencies
    runs-on: macos-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v4
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Test macOS dependency commands
        shell: bash
        run: |
          echo "🔍 Testing macOS dependency commands..."
          make -n --dry-run check-macos-deps 2>&1 || echo "⚠️  check-macos-deps has warnings (expected)"
          echo "✅ macOS dependency test completed"

  test-dependencies-windows:
    name: Test Windows Dependencies
    runs-on: windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v4
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Test Windows environment
        shell: bash
        run: |
          echo "🔍 Testing Windows environment..."
          which make || echo "⚠️  Make not found (expected on Windows)"
          which go || { echo "❌ Go not found"; exit 1; }
          echo "✅ Windows environment test completed"

  # --- TEST BUILD COMMANDS (DRY RUN) ---
  test-build-commands:
    name: Test Build Commands (Dry Run)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v4
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Install basic dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y make git

      - name: Test Windows build commands
        run: |
          echo "🔍 Testing Windows build commands..."
          make -n --dry-run windows 2>&1 || { echo "❌ windows target failed"; exit 1; }
          make -n --dry-run windows-arm64 2>&1 || { echo "❌ windows-arm64 target failed"; exit 1; }
          echo "✅ Windows build commands are valid"

      - name: Test Linux build commands
        run: |
          echo "🔍 Testing Linux build commands..."
          make -n --dry-run linux 2>&1 || { echo "❌ linux target failed"; exit 1; }
          echo "✅ Linux build commands are valid"

      - name: Test macOS build commands
        run: |
          echo "🔍 Testing macOS build commands..."
          make -n --dry-run macos 2>&1 || { echo "❌ macos target failed"; exit 1; }
          echo "✅ macOS build commands are valid"

      - name: Test installer commands
        run: |
          echo "🔍 Testing installer commands..."
          make -n --dry-run installer-windows 2>&1 || echo "⚠️  installer-windows has warnings (expected)"
          make -n --dry-run installer-linux 2>&1 || echo "⚠️  installer-linux has warnings (expected)"
          make -n --dry-run installer-macos 2>&1 || echo "⚠️  installer-macos has warnings (expected)"
          echo "✅ Installer commands test completed"

  # --- ACTUAL BUILD TEST (SMALL SCALE) ---
  test-actual-build:
    name: Test Actual Build (Small Scale)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v4
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y make git gcc || { echo "❌ Failed to install dependencies"; exit 1; }

      - name: Test Go modules
        run: |
          echo "🔍 Testing Go modules..."
          if [ -f "go.mod" ]; then
            go mod download || { echo "❌ Failed to download Go modules"; exit 1; }
            go mod verify || { echo "❌ Failed to verify Go modules"; exit 1; }
            echo "✅ Go modules are valid"
          else
            echo "⚠️  go.mod not found, skipping module test"
          fi

      - name: Test basic compilation
        run: |
          echo "🔍 Testing basic Go compilation..."
          if [ -f "cmd/main.go" ]; then
            go build -o /tmp/test-build ./cmd/main.go 2>&1 || echo "⚠️  Main compilation test failed (may be expected due to dependencies)"
            
            if [ -f "/tmp/test-build" ]; then
              echo "✅ Basic compilation test passed"
              rm /tmp/test-build
            else
              echo "⚠️  Binary not created, but continuing..."
            fi
          else
            echo "⚠️  cmd/main.go not found, skipping compilation test"
          fi

      - name: Test Makefile clean target
        run: |
          echo "🔍 Testing clean target..."
          make clean || { echo "❌ Clean target failed"; exit 1; }
          if [ ! -d "build" ] && [ ! -d "release" ]; then
            echo "✅ Clean target works correctly"
          else
            echo "⚠️  Build directories still exist after clean"
          fi

      - name: Test version generation
        run: |
          echo "🔍 Testing version generation..."
          make -n --dry-run changelog 2>&1 || { echo "❌ changelog target failed"; exit 1; }
          echo "✅ Version generation commands are valid"

  # --- CROSS-PLATFORM COMPATIBILITY TEST ---
  test-cross-platform:
    name: Test Cross-Platform - ${{ matrix.target }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        target: [windows, linux, macos]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v4
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Test cross-compilation for ${{ matrix.target }}
        run: |
          echo "🔍 Testing ${{ matrix.target }} cross-compilation..."
          make -n --dry-run ${{ matrix.target }} 2>&1 || { echo "❌ ${{ matrix.target }} target failed"; exit 1; }
          echo "✅ ${{ matrix.target }} cross-compilation test passed"

  # --- INTEGRATION TEST WITH MAKE MENU ---
  test-make-menu:
    name: Test Make Menu System
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v4
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Test menu display
        run: |
          echo "🔍 Testing menu display..."
          make -n --dry-run package 2>&1 || { echo "❌ package target failed"; exit 1; }
          echo "✅ Menu display test passed"

      - name: Test individual menu options
        run: |
          echo "🔍 Testing individual menu options..."
          make -n --dry-run release 2>&1 || { echo "❌ release target failed"; exit 1; }
          make -n --dry-run list-targets 2>&1 || { echo "❌ list-targets failed"; exit 1; }
          echo "✅ Menu options test passed"

      - name: Test help command
        run: |
          echo "🔍 Testing help command..."
          make help || { echo "❌ help target failed"; exit 1; }
          echo "✅ Help command test passed"

  # --- VALIDATE BUILD STRUCTURE ---
  test-build-structure:
    name: Test Build Structure
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Validate project structure
        run: |
          echo "🔍 Validating project structure..."
          
          # Check for essential files
          [ -f "Makefile" ] || { echo "❌ Makefile not found"; exit 1; }
          [ -f "go.mod" ] || echo "⚠️  go.mod not found (may be expected)"
          [ -d "cmd" ] || echo "⚠️  cmd directory not found"
          
          echo "✅ Project structure validation completed"

      - name: Test Makefile phony targets
        run: |
          echo "🔍 Testing .PHONY declarations..."
          grep -q "^\.PHONY:" Makefile || { echo "❌ .PHONY declaration not found"; exit 1; }
          echo "✅ .PHONY targets are declared"

  # --- FINAL REPORT ---
  generate-report:
    name: Generate Test Report
    runs-on: ubuntu-latest
    needs: 
      - test-makefile-syntax
      - test-dependencies-linux
      - test-dependencies-macos
      - test-dependencies-windows
      - test-build-commands
      - test-actual-build
      - test-cross-platform
      - test-make-menu
      - test-build-structure
    if: always()
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Generate test report
        run: |
          echo "📊 ==========================================="
          echo "📊 MAKEFILE TEST REPORT - $(date)"
          echo "📊 ==========================================="
          
          # Check job results
          if [ "${{ needs.test-makefile-syntax.result }}" == "success" ] && \
             [ "${{ needs.test-dependencies-linux.result }}" == "success" ] && \
             [ "${{ needs.test-build-commands.result }}" == "success" ] && \
             [ "${{ needs.test-actual-build.result }}" == "success" ] && \
             [ "${{ needs.test-cross-platform.result }}" == "success" ] && \
             [ "${{ needs.test-make-menu.result }}" == "success" ] && \
             [ "${{ needs.test-build-structure.result }}" == "success" ]; then
            echo "📊 ✅ All critical Makefile tests passed!"
          else
            echo "📊 ⚠️  Some tests failed or were skipped"
          fi
          
          echo "📊 "
          echo "📊 Test Summary:"
          echo "📊 - Makefile syntax: ${{ needs.test-makefile-syntax.result }}"
          echo "📊 - Linux dependencies: ${{ needs.test-dependencies-linux.result }}"
          echo "📊 - macOS dependencies: ${{ needs.test-dependencies-macos.result }}"
          echo "📊 - Windows dependencies: ${{ needs.test-dependencies-windows.result }}"
          echo "📊 - Build commands: ${{ needs.test-build-commands.result }}"
          echo "📊 - Actual build: ${{ needs.test-actual-build.result }}"
          echo "📊 - Cross-platform: ${{ needs.test-cross-platform.result }}"
          echo "📊 - Menu system: ${{ needs.test-make-menu.result }}"
          echo "📊 - Build structure: ${{ needs.test-build-structure.result }}"
          echo "📊 "
          echo "📊 ==========================================="

      - name: Upload test artifacts
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: makefile-test-report
          path: |
            Makefile
          retention-days: 30